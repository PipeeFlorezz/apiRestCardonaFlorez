import { AlumnosGeneroDirective } from './alumnos-genero.directive';

describe('AlumnosGeneroDirective', () => {
  it('should create an instance', () => {
    const directive = new AlumnosGeneroDirective();
    expect(directive).toBeTruthy();
  });
});
